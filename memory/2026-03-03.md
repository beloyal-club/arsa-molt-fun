# 2026-03-03

## Discord OAuth Authentication Fix (IN PROGRESS)

### Problem
Discord bot (Breth) shows "No API key found for provider anthropic" error BEFORE each response, even though responses work. Two code paths firing: one fails auth → sends error, one succeeds → sends response.

### Root Cause Investigation
- Environment has BOTH `ANTHROPIC_API_KEY` and `ANTHROPIC_OAUTH_TOKEN` set from Worker
- OpenClaw may be checking env var first (fails?), then auth-profiles (succeeds)
- Or webhooks/duplicate message handling firing twice

### Auth Profile Format (Correct)
```json
{"profiles":{"anthropic:default":{"type":"token","provider":"anthropic","token":"sk-ant-oat01-..."}}}
```

**Location:** Must exist in ALL 3 locations:
- `/root/.openclaw/auth-profiles.json`
- `/root/.openclaw/agents/main/auth-profiles.json`
- `/root/.openclaw/agents/main/agent/auth-profiles.json`

### Config Changes Made
1. Fixed malformed `auth-profiles.json` (had nested `profiles.auth.profiles`)
2. Added `requireMention: false` to guild config
3. Updated KV config at `channels/discord` to persist settings
4. Changed auth format from `mode: "api_key"` to `type: "token"`
5. Added `auth.order.anthropic` config to prioritize `anthropic:default` profile

### Key Config Paths
- KV Namespace: `177485fca6a54ac7bafe23498b2f6eba`
- Guild ID: `1064272702254354434`
- Channel ID: `1475231662332969192`

### Next Steps If Error Persists
1. Modify Worker's `buildEnvVars()` in `src/gateway/env.ts` to NOT pass `ANTHROPIC_API_KEY` when using OAuth
2. Or unset `ANTHROPIC_API_KEY` env var, keep only `ANTHROPIC_OAUTH_TOKEN`
3. Trace log paths to identify why two separate systems handle the same message

### Worker Env Var Passthrough (Reference)
File: `src/gateway/env.ts` in `beloyal-club/arsa-molt-fun` repo
- `ANTHROPIC_API_KEY` currently passed to container
- May need to conditionally skip if OAuth token is preferred
